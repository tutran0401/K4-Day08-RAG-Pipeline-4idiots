# Core
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0

# Task 3 - Document conversion
markitdown[pdf]>=0.1.0
fpdf2>=2.7.0

# Optional production backends used by Task 4/5
chromadb>=0.5.0

# Set RAG_EMBEDDING_BACKEND=sentence_transformer (or the compatible
# EMBEDDING_PROVIDER=sentence_transformers alias) to use local model embeddings.
sentence-transformers>=2.2.0

# Task 6 - BM25 + TF-IDF
rank-bm25>=0.2.2
scikit-learn>=1.3.0

# Task 10 - Generation
openai>=1.30.0

# Group project - UI
streamlit>=1.35.0

# Testing (dung de cham diem bai ca nhan)
pytest>=7.0.0

# Optional online crawler and legacy RAGAS judge dependencies are isolated in
# requirements-crawl.txt and requirements-ragas.txt because they require
# incompatible major OpenAI/LangChain dependency branches.
